// إعدادات قاعدة البيانات (للتوسع المستقبلي)

const databaseConfig = {
    // للإصدار الحالي نستخدم التخزين في الذاكرة
    // في المستقبل يمكن إضافة إعدادات MySQL, MongoDB, etc.
    
    development: {
        type: 'memory',
        description: 'تخزين مؤقت في الذاكرة'
    },
    
    production: {
        type: 'memory',
        description: 'تخزين مؤقت في الذاكرة - للتغيير لاحقاً'
    }
};

// دالة لتهيئة قاعدة البيانات
const initializeDatabase = () => {
    console.log('🔄 جارٍ تهيئة نظام التخزين...');
    console.log('📝 نظام التخزين الحالي: في الذاكرة');
    console.log('💡 ملاحظة: للاستخدام المنتج، يرجى استخدام قاعدة بيانات حقيقية');
    
    return {
        type: 'memory',
        connected: true,
        timestamp: new Date().toISOString()
    };
};

module.exports = {
    databaseConfig,
    initializeDatabase
};